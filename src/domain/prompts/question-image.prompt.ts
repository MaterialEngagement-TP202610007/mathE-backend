export function buildQuestionImagePrompt(statement: string): string {
  return `Colorful educational illustration for a Peruvian school activity. Scene: "${statement}". Child-friendly art style, vibrant colors, no text or labels.`;
}
